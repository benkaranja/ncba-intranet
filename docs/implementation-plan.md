# NCBA Ubuntu Hub — Implementation Plan

**Project:** Static HTML/CSS mockup of a redesigned NCBA Group SharePoint intranet ("Ubuntu Hub")
**Type:** Pitch artifact — must look and click like the real thing from `file://`. No SharePoint deployment code.
**Started:** 2026-08-06
**Status key:** ☐ not started · ◐ in progress · ☑ done · ⊘ blocked

This document is the single source of truth for the build. It is updated as work proceeds.

---

## 1. Ground rules (non-negotiable)

| # | Rule | How it is enforced |
|---|---|---|
| 1 | The forbidden competitor name must not appear in any output file, in any case | `docs/check.py` greps the whole tree in Phase 4. **This document deliberately never spells the name out** — the acceptance grep covers `docs/` too, so writing it here would fail the check. `check.py` assembles it from fragments for the same reason. |
| 2 | No fabricated assets — every `<img src>` resolves to a real file | All images generated by script from measured sources; automated asset check in Phase 4 |
| 3 | One locked dimension per image slot | Three slots (§4); every file written by the same `cover()` routine at a fixed size |
| 4 | Static only — hand-written HTML + one CSS file, relative paths | No build step, no framework, no CDN. Only JS is one inline carousel script, shared by every rotator (D-054) |
| 5 | Build only what is specified | No extra pages, no abstractions |
| 6 | Never modify or delete anything in `Reference/` or `Images/` | Sources are opened read-only; all output goes to `assets/`, `docs/`, `brand/`, and the nine root HTML files |

---

## 2. Source-path corrections

The brief's paths did not match disk. These are the real ones, used throughout:

| Brief said | Actual path |
|---|---|
| `PAGES/` | `Reference/PAGES/` |
| `Intranet Banners and Posts/` | `Reference/Intranet Banners and Posts/` |
| `PAGES/@NCBA Intranet - Group.html` | `Reference/PAGES/NCBA Intranet - Group.html` |
| `Images/15 Louisa Wandabwa 1.png` | `Images/departments/15 Louisa Wandabwa 1.png` |
| `Images/Strategy Video Picture.png` | ✅ as stated |
| *(not in brief)* | `Images/updated intranet website banners images/` — 30 newer assets, added mid-build. **Preferred over `Reference/Intranet Banners and Posts/` where a filename appears in both.** |

---

## 3. Target file tree

| Path | Level | Description | Status |
|---|---|---|---|
| `index.html` | L1 | Group home — hero slider, GMD/HR two-up, UBUNTU Spirit, updates carousel, news | ☑ |
| `ubuntu-hub.html` | L1 | UBUNTU Hub — Spirit video, Manifesto, strategy on a page, Louisa | ☑ |
| `staff.html` | L1 | Staff — Existing Staff and New Staff sections | ☑ |
| `connect-with-john.html` | L1 | GMD page — Ask-the-GMD composer + 5 video articles | ☑ |
| `region-kenya.html` | L2 | Kenya — country banner + CSS flag, GMD video, HR banner | ☑ |
| `kenya-mcc.html` | L3 | Marketing, Communications & Citizenship | ☑ |
| `kenya-strategy.html` | L3 | Strategy | ☑ |
| `kenya-hr.html` | L3 | Human Resources — plus its own HR carousel | ☑ |
| `article.html` | L4 | Full post view | ☑ |
| `assets/css/ncba.css` | — | The single stylesheet | ☑ |
| `assets/img/` | — | Processed images, 3 locked sizes | ☑ (49 files) |
| `docs/*.md`, `brand/*.md` | — | Plan, decisions, specs, handoff, brand guide | ☑ |

---

## 4. Locked image slots

Three slots (the `sidebar-tile` slot was retired in D-042). Every instance of a slot uses exactly one dimension. Full detail in `docs/component-specs.md`.

| Slot | Locked size | Ratio | Used by |
|---|---|---|---|
| `hero-banner` | **1920 × 310** | 6.19:1 | Page header banner, all 7 pages, full-bleed |
| `media-16x9` | **940 × 529** | 16:9 | Carousel slides, video thumbnails, full-width feature images, article hero |
| `loop-thumb` | **320 × 180** | 16:9 | Blog-post loop side image (news + video variants) |

**Crop rules (verified visually before locking):**
- 16:9-native sources (1920×1080) → **no crop** at `media-16x9`. Chosen specifically so KV headline text and the "Go for it" badge are never clipped.
- 1:1 sources (1080×1080 ESHOT/POST) → **top-anchored** crop. Centre-cropping decapitates the headlines; top-anchoring keeps headline + face and drops only the lower CTA line.
- Photographs → centre crop, anchored 0.18 from top at `hero-banner` so the subject's face lands in the band.

---

## 5. Global navigation

**Main nav** (v2, decision D-053) — six items:

`About NCBA` · `Staff ▾` · `Culture & Change` · `Departments ▾` · `UBUNTU Hub` · `NCBA Group ↗`

- **Staff ▾** → Existing Staff · New Staff (deep links into `staff.html`)
- **Departments ▾** → MCC · Strategy · Human Resources · UBUNTU Hub
- **NCBA Group** → `https://ncbagroup.com/` — the one whitelisted external URL (D-057)
- **Connect with John** is no longer in the header; the page remains, reached via **Ask John** in the sidebar

**Secondary nav:** the four department pages carry a Departments strip; `region-kenya.html` retains the country strip with department dropdowns.

**Link policy:** every `href` resolves to one of the nine files, to a real in-page anchor, or to the single whitelisted external URL. Nothing else passes `check.py`.

---

## 6. Sidebar widget order

Identical on every page (D-055):

1. **Quick Links** — Group sidebar: UBUNTU Strategy · Success Factors · Brand Manifesto · Ask John · Financials · Culture Page. Regional sidebar: Group · UBUNTU Strategy · Document Approval · Merchandise Hub · Rate My Service · Daraja
2. **Upcoming events** — SharePoint's default events widget
3. **NCBA Staff Updates** — Viva Engage feed on the Monday Blues / Midweek Fatigue / Furahi Day themes

---

## 7. Phased build order

### Phase 0 — Inventory ☑
- ☑ Inventory `Reference/PAGES/` (7 exports), extract nav, footer state, component patterns
- ☑ Measure every image in `Reference/`, `Images/`, and the updated banners folder
- ☑ Sample brand colours from the artwork rather than guessing
- ☑ Confirm sources are free of the banned string (0 hits)

### Phase 1 — Clarifications ☑
- ☑ 4 questions asked and answered (see `docs/decisions.md` D-004…D-007)

### Phase 2 — Documentation ☑
- ☑ `docs/implementation-plan.md` (this file)
- ☑ `brand/NCBA-brand-guide.md`
- ☑ `docs/component-specs.md`
- ☑ `docs/decisions.md`
- **Gate: user sign-off before any HTML is generated**

### Phase 3a — Pattern-setter ☑
- ☑ Generate `assets/img/**` from sources at the 4 locked sizes
- ☑ Extract transparent white NCBA logo → `assets/img/ncba-logo-white.png`
- ☑ Write `assets/css/ncba.css` (tokens → base → chrome → layout → components → responsive)
- ☑ Write `index.html` with all 7 blocks in the specified order
- **Gate: user sign-off on look and feel before the remaining 6 pages**

### Phase 3b — Remaining pages ☑
Built strictly against the index.html pattern; no new component types.
- ☑ `connect-with-john.html`
- ☑ `region-kenya.html`
- ☑ `kenya-strategy.html`
- ☑ `kenya-mcc.html`
- ☑ `kenya-hr.html`
- ☑ `article.html`

### Phase 4 — Verification and handoff ☑
- ☑ Forbidden-name grep over the whole tree → must return nothing
- ☑ Automated link + asset checker: every `href` and `src` in all 7 pages resolves to a real file or a real in-page anchor id
- ☑ Assert every rendered image file matches its slot's locked dimension
- ☑ Assert sidebar widget order per page type
- ☑ Reconcile `brand/NCBA-brand-guide.md` against the values actually in `ncba.css`
- ☑ Render each page and check visually
- ☑ Write `docs/handoff.md`
- ☑ Finalise `docs/decisions.md` and `docs/component-specs.md`

---

## 8. Page block order (v2)

**`index.html`** — hero slider (4 banners) · GMD + HR two-up · The UBUNTU Spirit · updates carousel · news loop · We're here to help · footer

**`ubuntu-hub.html`** — banner · intro · UBUNTU Spirit video · The UBUNTU Manifesto · strategy on a page · Louisa feature · document tiles · news loop · help · footer
*(No BUST section — no such asset exists yet, D-058.)*

**`staff.html`** — banner · intro · Existing Staff section + tiles · New Staff section + tiles · news loop · help · footer

**`connect-with-john.html`** — CEO banner · intro · Ask-the-GMD composer · 5 video articles · help · footer

**`region-kenya.html`** — country banner + CSS flag · GMD video · Monicah Kihia banner · news loop · help · footer

**`kenya-mcc` / `kenya-strategy`** — banner · intro · department feature · document tiles · news loop · help · footer *(Strategy also closes with its video.)*

**`kenya-hr.html`** — banner · intro · **HR carousel** · HR Mission feature · document tiles · news loop · help · footer

**`article.html`** — hero · title · byline · body · pull quote · share row · related strip · help · footer

---

## 9. Risks and mitigations

| Risk | Mitigation |
|---|---|
| Banned string leaks in via copied markup | Nothing is copied from the reference screenshot; all copy is newly written; Phase 4 grep gates delivery |
| Image slot drift | All images written by one script at four fixed sizes; Phase 4 asserts dimensions on disk |
| Dead links from placeholder `href="#"` | Every placeholder points at a real anchor id that exists in the page; checker validates anchor targets too |
| Brand guide drifting from CSS | Guide is written first as the spec, then reconciled against `ncba.css` in Phase 4 before sign-off |
| Nav overflow at 10 items | 14px/600 at 1300px canvas fits; below 1024px the nav scrolls horizontally rather than wrapping |
